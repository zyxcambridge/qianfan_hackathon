import { ResponseInterceptor } from '../../types/interceptors';
import { Messages } from '../../views/chat/messages/messages';
import { StreamHandlers } from '../../services/serviceIO';
type Finish = () => void;
export declare class Demo {
    static readonly URL = "deep-chat-demo";
    private static generateResponse;
    private static getCustomResponse;
    private static getResponse;
    static request(messages: Messages, onFinish: Finish, responseInterceptor?: ResponseInterceptor): void;
    static requestStream(messages: Messages, sh: StreamHandlers): void;
}
export {};
//# sourceMappingURL=demo.d.ts.map